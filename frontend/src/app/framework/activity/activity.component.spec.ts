import { TestBed } from '@angular/core/testing';
import { ActivityComponent } from './activity.component';

describe('ActivityComponent', () => {
  it('should create', () => {
    expect(ActivityComponent).toBeTruthy();
  });
});
