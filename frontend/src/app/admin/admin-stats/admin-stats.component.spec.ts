import { TestBed } from '@angular/core/testing';
import { AdminStatsComponent } from './admin-stats.component';

describe('AdminStatsComponent', () => {
  it('should create', () => {
    expect(AdminStatsComponent).toBeTruthy();
  });
});
