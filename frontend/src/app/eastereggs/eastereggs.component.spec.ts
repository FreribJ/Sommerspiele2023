import { TestBed } from '@angular/core/testing';
import { EastereggsComponent } from './eastereggs.component';

describe('EastereggsComponent', () => {
  it('should create', () => {
    expect(EastereggsComponent).toBeTruthy();
  });
});
