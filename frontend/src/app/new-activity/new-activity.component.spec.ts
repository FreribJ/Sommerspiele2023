import { TestBed } from '@angular/core/testing';
import { NewActivityComponent } from './new-activity.component';

describe('NewActivityComponent', () => {
  it('should create', () => {
    expect(NewActivityComponent).toBeTruthy();
  });
});
