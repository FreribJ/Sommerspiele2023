import { TestBed } from '@angular/core/testing';
import { GuessingComponent } from './guessing.component';

describe('GuessingComponent', () => {
  it('should create', () => {
    expect(GuessingComponent).toBeTruthy();
  });
});
