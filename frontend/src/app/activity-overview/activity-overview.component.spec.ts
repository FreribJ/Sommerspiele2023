import { TestBed } from '@angular/core/testing';
import { ActivityOverviewComponent } from './activity-overview.component';

describe('ActivityOverviewComponent', () => {
  it('should create', () => {
    expect(ActivityOverviewComponent).toBeTruthy();
  });
});
